# **프로젝트명** : 가시숲의 아이

**개발 기간** : 26.02.26 ~ 26.03.06

**목표** : State Pattern, Scriptable Object(SO), JSON(Save/Load), MVP 등을 활용한 기능 구현 — 배운 내용 적용

**만들고자 한 것** : 메트로베니아 스타일 2D 액션 게임

---

## **잘한 점**

1. **상태 패턴 일관 적용**프로젝트의 80% 이상이 상태 패턴으로 구성되어 있다. “상태 = 하나의 클래스”라고 정리하고 가서, 플레이어(Idle/Move/Jump/Fall/Attack/AirAttack/Dead)와 적(Patrol/Chase/Attack/Dead, 비행형은 FlyPatrol/FlyChase) 모두 같은 방식으로 확장할 수 있었다.

---

## **아쉬운 점**

1. **시간 배분**
    
    혼자 진행하다 보니 스스로에게 관대해지고 나태해진 부분이 있었다.
    
2. **프로젝트 규모**
    
    생각보다 규모가 크고 다루어야 할 요소가 많았다.
    
3. **기능 하나에 대한 과한 투자**
    
    한 기능을 완성하는 데 시간을 너무 많이 쓰는 경우가 있었다.
    
4. **연출 부족**
    
    연출까지 손대지 못했다. 이 부분은 제대로 공부하지 못한 것이 가장 아쉽다.
    

---

## **문제 해결 과정**

### **1. 점프 중 공격 (AirAttackState)**

- **원인**
    
    공중 상태(Jump/Fall)에서 공격 입력을 보지 않아, 공격 전환이 “착지 → 지상 → 공격”으로만 일어났다.
    
- **해결**
    - 공중 상태에서 AttackInput이 들어오면 곧바로 **AirAttackState**로 전환
    - 공중 공격은 **별도 상태(AirAttackState)**로 두어 지상 공격(AttackState)과 역할을 분리
- **설계**
    
    “점프 중 공격”을 AttackState에 섞지 않고 AirAttackState로 분리해, 상태 단위를 명확히 했다. 나중에 공중 전용 연출(예: AttackUp)을 붙이기도 쉬운 구조다.
    

---

### **2. 연속 공격(콤보) 처리**

Attack 로직을 만들어 두고 애니메이션만 연결하면 될 줄 알았는데, 한 번 휘두르고 끝이거나 X키 연타에 반응하지 않았다.

- **콤보 인덱스**
    
    `ComboIndex`(0=Attack1, 1=Attack2, 2=Attack3), `ComboLength = 3`으로 1→2→3→1 순환.
    
- **진행**
    
    `AttackState` / `AirAttackState`의 Update에서 **공격 입력(AttackInput)이 들어올 때마다**
    
    - `_hasHit = false`로 리셋 (다음 타격 허용)
    - `AdvanceCombo()`로 다음 콤보 단계로 이동
    - `TriggerAttack()`으로 해당 단계 애니메이션 트리거
    - `ConsumeAttack()`으로 입력 소비
- **리셋**
    
    `IsAttackComplete()`(애니메이션 normalizedTime ≥ 0.95 또는 공격 상태 아님)일 때만 `ResetCombo()` 호출.
    
    → X키를 타이밍 맞춰 누르면 다음 공격으로 이어지고, 안 누르면 애니가 끝날 때만 콤보가 리셋되도록 했다.
    

---

### **3. 공격 범위 판정 — “팔 끝에만 닿아야 데미지가 들어가던” 문제**

- **원인**
    
    판정이 한 프레임이나 극히 짧은 순간에만 있어서, 팔 끝이 정확히 적에 닿은 순간만 히트했다.
    
- **해결**
    
    **히트 윈도우**로 구간 판정을 넣었다.
    
    - `_hitWindowStart`, `_hitWindowEnd`(normalizedTime 구간)에서만 `IsInAttackHitWindow()`가 true
    - 그 구간 안에서만 `TryHitEnemies()`를 호출해, “팔이 나가는 동안” 일정 시간 동안 판정이 열리도록 함
    
    추가로, **판정을 플레이어 앞쪽 박스**로 두었다.
    
    - `_attackRange`, `_attackHeight`, `_attackOffsetX`로 크기·위치를 잡고
    - `Physics2D.OverlapBoxAll`로 그 박스 안의 IDamageable에게만 데미지→ 팔 길이에 덜 의존하고, 플레이감이 나아졌다.

---

## **미해결**

- **점프 → 낙하 애니메이션**
    
    점프 상승(Raise) 후 낙하(Fall) 로직은 구현했지만, 애니메이션 연결이 어려워 포기했다.
    
- **FireAttack 스킬**
    
    FireAttack 스킬을 넣으려 했으나, 기존 캐릭터에 해당 애니메이션이 없었다.
    
    다른 캐릭터에서 애니를 가져와 쓰면 스프라이트가 바뀌는 문제가 있어, 오버라이드로 해결을 시도했으나 완전히 원하는 대로 동작하지 않았고, 다른 작업에 비해 시간을 너무 쓰는 것 같아 중단했다.
    
- **캐릭터가 몬스터 머리 위에 올라가는 현상**
    
    박스 콜라이더/레이어 설정 문제로, 캐릭터가 몬스터 머리 위에 올라가는 현상을 해결하지 못했다.
    

---

## **추가 및 보완점**

- 보스 몬스터 추가
- 연출 강화
- **SO 활용 확대**몬스터 체력, 공격력, 공격 범위, 추적 범위 등은 `EnemyData` / `FlyData` SO로 공통화해 두었는데, 이 데이터 설계를 더 확장해서 다양한 몬스터/스킬/스테이지 데이터를 SO로 짜 보고 싶었다. 이 부분이 가장 아쉽다.
- 메인화면을 MVP 분리 해줬는데 생각해보니 화면에 보여지는것도 없어서 그냥  `MainMenuController.cs` 같은 컨트롤러 하나로만 관리 했어도 될 것 같았다. (+ 솔직히 사용해 보고 싶었습니다.)

---

## **총평**

1. 기본 틀은 내가 정함
2. 로직 구현은 AI와 함께 진행
3. 검사해서 걸러 낼 부분은 걸러 내고, 검사 후 직접 작성
4. 기능 하나 완성 (+ 기능 간 연결)
5. 위 과정을 반복

이런 식으로 프로젝트를 진행했다.